programa testeMedia;
var
    cont, x, Media : inteiro;
    bool: logico;
início
    cont:=VERDADEIRO;
    cont:=3+bool;
    Media:=0;
    enquanto (cont<>5) faca
    início
        escrever('Valor ',cont,': ');
        ler(x);
        Media := Media + x;
        cont:=cont+1;
    fim
    escrever('Soma dos valores: ');
    escrever(Media);
    escrever('Media dos valores: ');
    escrever(Media/5);
fim?